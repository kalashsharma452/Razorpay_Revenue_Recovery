services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: razorpay
      POSTGRES_PASSWORD: razorpay
      POSTGRES_DB: recovery
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
